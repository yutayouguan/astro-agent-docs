# Codex 源码对齐更新说明

> 日期：2026-09-03
> 状态：已落地并完成聚焦验证
> Codex 比较基线：`e24190caa9..a0dcfe2ada`
> Astro 实现提交：`0d8d3b25`、`34985a7d`、`5c92f99a`、`71b4eee7`、`3c0be21d`、`25f1798e`、`65a95eef`、`db97f12f`、`1735a7dd`、`238cff34`、`87b229f5`、`9f04e810`、`4563aad2`、`74f0c9b1`

## 更新摘要

| 领域 | Astro 变更 | 对齐边界 |
| --- | --- | --- |
| 异步用户输入 | `request_user_input_async` + durable `questions` + Desktop ClarifyWizard | 对齐 Codex 结构化问题语义；旧名不再注册 |
| Thread 恢复 | Snapshot 暴露 nullable `model` / `reasoning_effort` | Server 当前设置是权威源，Desktop 不反向覆盖 |
| Worktree | 独立 `agent-delegate` crate + detached managed checkout | 只用于显式桌面任务；Subagent 不隐式创建 |
| Realtime | 增加连续 session 边界与 rollout 恢复回归 | 保持 Astro V2/V3 现有 wire 与传输语义 |
| Subagent tier | root-scoped service tier 继承 | 仅向 OpenAI/Codex backend 传递，不放宽子 Agent 权限 |
| Agent Tree | 快照携带 nullable 根 service tier，Desktop 状态投影与摘要可见 | 保持 root scope；不冒充 Codex daemon-wide Agents Overview |
| MCP 输出 | 单工具 `output_token_limit` | 配置/DTO/Registry/原始结果/PostToolUse 全链保留最终预算 |
| 工具命名空间 | Responses 原生 `(namespace, name)` | Schema、StepContext、MCP、ToolSearch、output、SQLite 和 UI 全链保留；删除展平直调兼容 |
| 内置能力分组 | `browser`、`media`、`cron` | Browser 与媒体生成纳入原生 namespace；媒体理解/机器人工具保持独立 |
| Desktop 目录对齐 | API name / namespace / registered name 分离 | ToolsPanel 展示模型真实调用名，toolset 开关 id 保持不变 |
| Persistent reasoning | 模型目录能力、Desktop 档位、gRPC 与 OpenAI Responses wire 全链 | 仅非空 `persistent_instructions` 可见；wire effort 为 `disabled`；其他 Provider 拒绝 |

## 1. 结构化异步用户输入

### 工具契约

```json
{
  "name": "request_user_input_async",
  "arguments": {
    "questions": [
      {
        "title": "需要用户回答的完整问题",
        "options": ["建议答案 A", "建议答案 B"]
      }
    ]
  }
}
```

- 问题数组不能为空，`title` 不能是空白文本。
- `options` 可省略；Desktop 始终允许自由文本。
- 工具立即返回 `{"accepted":true}`，不结束、不 park 当前 turn。
- 可读 fallback text 与结构化 `questions` 一起进入 `AgentMessageItem`。
- `ItemStarted` / `ItemCompleted` 共用稳定 ID，rollout 恢复和 Desktop 投影按 ID 去重。
- 仅最新、尚未被后续 user input 回答的问题组保持交互性。

`send_user_message_async` 已从 handler、工具注册、Plan 模式白名单和工具开关迁移中移除。
持久化的旧调用如果被重放，会按未知工具明确失败，不再静默改写。

## 2. Thread Snapshot 模型设置

`ThreadSnapshot` 新增：

```proto
optional string model = 8;
optional string reasoning_effort = 9;
optional string provider_id = 10;
optional string backend_id = 11;
```

listener 在重放 rollout 时同时跟踪最新 `ThreadSettingsApplied`，不需要重启
Agent turn 即可恢复 provider/backend/model/reasoning。已加载 Session 只补齐缺失值。

## 3. Managed worktree

`agent-delegate` 恢复为独立 workspace crate，不再把 Git 检出逻辑放在 `agent-core`。

```text
source cwd
  -> canonical repository root + nested cwd
  -> verify HEAD/explicit base commit
  -> allocate managed bucket
  -> git worktree add --detach --no-checkout
  -> reset --hard with hooks/fsmonitor/filters disabled
  -> validate destination cwd
  -> ManagedWorktree
```

安全边界：

- 清除继承的 Git repository selectors，防止命令被父进程环境重定向。
- 不执行仓库 hooks、filesystem monitor 和 clean/smudge/process filters。
- 目标 cwd 必须解析到 managed root 内的真实目录。
- 失败时回滚不完整 worktree 和 allocation bucket。
- 脏 worktree 不自动删除。
- Desktop cleanup 会重新解析 repo root，且只允许删除
  `.worktrees/<bucket>/<repo>` 下的 Astro 管理检出。
- Git 命令强制 `safe.bareRepository=explicit`，避免隐式 bare repository 选择。
- `.worktreeinclude` 兼容入口只复制普通文件/目录，跳过绝对路径、`..` 和符号链接。

## 4. Realtime 跨 session 恢复

`RealtimeHistory::start` 在已有 active session 时使用下列顺序收敛：

1. 封口旧 session 未完成的 user/assistant transcript；
2. 追加旧 session closed outcome；
3. 追加新 session started。

`agent_rollout::realtime_history()` 只筛选 durable `RealtimeItem`，但保持追加顺序，
因此连续会话恢复时不会合并或重排 session boundary。

## 5. 已有能力的验收结论

以下 Codex 变化在 Astro 已有等价语义，本轮只做源码/测试确认：

- User / AutoReview / Full Access 三维权限路由，以及 hardline deny 优先级。
- 本地 Extension 每 turn 重新发现，`ExtensionSnapshot` 在 turn 内冻结。
- `McpHub::reload_with_configs` 保留未变连接，重连变更项，移除已删除/禁用项。
- MCP `tools/list_changed` 自动更新工具快照，elicitation 使用有界 broker。

远端 Plugin Marketplace 与原生 Voice helper 已完成独立设计审查。Marketplace 需要 Astro
自有的服务、认证和 bundle 信任契约；native voice 需要签名的三平台 runtime 产物。两者在
这些前置条件满足前不暴露虚假入口，设计与验收门槛见文末链接。

## 6. Persistent reasoning

- `ModelReasoningMeta` 携带可空 `persistent_instructions`。模型目录只有提供非空指令时，
  Desktop 才显示 `persistent` 档位；缓存 enrich 仅为 OpenAI 保留该字段。
- Desktop 将目录指令随 `ChatRequest` 发送；Server 在初始配置和 active-turn 切换时同时校验
  OpenAI backend 与非空指令，缺一即返回 `InvalidArgument`。
- Core 内部使用保留键 `astro_persistent_instructions` 保存指令；OpenAI Responses adapter
  消费并移除该键，把指令合并到请求 instructions，同时将本地 effort `persistent` 映射为
  wire effort `disabled`。
- Azure、OpenRouter、DeepSeek、自定义 Responses adapter 等其他 Provider 不声明该能力，
  收到 `persistent` 时明确失败，不做原样透传或静默降级。
- 切回其他 effort 后，缓存指令可以留在 session 内供后续再次启用，但不会注入普通请求。

## 7. Network header injection requirements

`permissions.profiles.<id>.network.header_injections` 接受 `host`、`methods`、
`path_prefixes` 和 `headers`，并随 active leaf profile 进入 attempt-scoped
`NetworkProxyState`。规则不改变 allow/deny 结果；Debug 仅输出 host、method、path 与 header
名称，绝不输出 header value。

本轮对齐的是 Codex `#42173` 的 requirements parse/carry/redaction 边界。Astro 当前 managed
proxy 是 HTTP CONNECT 隧道，无法读取 TLS 内部 method/path，因而不会对 HTTPS 流量做虚假
注入；只有未来具备明文 HTTP 元数据的 transport 才能消费这些规则。

## 8. Responses 原生工具命名空间

- `ToolName` 作为 `ToolRouter.routes` / `model_routes` 的结构化键；默认
  `functions` namespace 与无 namespace 等价。
- `ResponseItem::FunctionCall` 中的 `namespace` 和 `name` 直接进入当前
  StepContext 校验，不先展平成 `namespace.child` 或 `namespace__child`。
- MCP 模型可见形态为 `namespace=mcp__{server}` + 原生子工具；展平
  `mcp__{server}__{tool}` 仅作为 Hub 内部执行键。
- `tool_search` 会合并同 namespace 命中项，并由 durable `ToolSearchOutput`
  在下一 Step 重建可路由 `ToolName` 集合。
- Function-only 非 Agent adapter 不再把 Namespace 降级为伪 Function 名。
- Browser 投影为 `browser.<child>`，但 Plan/HITL 仍按内部 `browser_*`
  策略判定；媒体生成投影为 `media.image_gen`、`media.video_gen`、
  `media.speech_gen`、`media.music_gen`，并保持原 toolset 开关粒度。

## 9. 验证记录

- Agent / Protocol / Tools / Rollout / Server / Tauri 相关 Rust 测试通过。
- `cargo test -p worktree`：7 项通过。
- `cargo test -p realtime -p agent-rollout`：48 项通过。
- Desktop `npm test`：615 项通过。
- Desktop `npx tsc --noEmit` 和 `npm run build` 通过。
- 相关 crate 严格 Clippy 通过；全量 Server Clippy 仍受未涉及本次修改的既有 lint 阻塞。
- Subagent service-tier 共享/供应商门禁、MCP 配置 round-trip、原始输出截断与
  PostToolUse 后二次截断均有聚焦回归。
- Agent Tree 快照、snake/camel DTO 兼容、增量事件和 mark-read 的根 tier 保留均有回归。

## 10. 后续产品依赖设计

- Remote Extension Marketplace 对齐设计
- Native Voice Helper 对齐设计
