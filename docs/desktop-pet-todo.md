# 桌宠改进 TODO

按顺序推进；代码完成和原生验收分别记录。真实多屏、锁屏或初始化阻挡时保留待验收项，不自动修改用户配置来跳过。

- [ ] **1. 原生回归验收**（进行中）
  - 白底/深底；75%/100%/150%；无背景、阴影、遮脸按钮。
  - 主窗口单击、输入焦点、透明区域穿透、拖动和右键菜单。
  - 重启恢复、显示器切换与断开；记录真实通过和未测场景。
- [ ] **2. 位置锁定与贴边吸附**
  - [x] 设置和右键可锁定位置；移动结束后记住位置；吸附可关闭。
  - [x] 工作区坐标、负坐标、DPI、断屏回退几何测试；提供“回到屏幕内”。
  - [ ] 真实拖动/吸附/重启/多屏插拔验收。
- [ ] **3. 安静模式**
  - [x] 待机/眨眼为主，关闭自动大动作；可调频率，手动播放仍可用。
  - [x] 全屏/手动演示隐藏不改 enabled；托盘恢复及窗口实际可见状态接入。
  - [ ] 全屏进出、托盘恢复的原生复查。
- [ ] **4. 现有动作精修**
  - [x] 内置片段首尾改用完全相同的主图集中立帧；有回归测试，第三方片段默认不启用。
  - [x] 按运行时生效帧逐帧审查踩奶、舔爪；生成白/深底联系表、3倍慢放和循环边界测量。
  - [ ] 补齐入口真实过渡姿态，再完成待机/踩奶/舔爪连续播放及体积变化验收；当前入口自然度未通过。
  - 优先修时序与定位，确需缺失姿态再使用已授权图片模型；不以帧数或淡化重影冒充质量。
- [ ] **5. 场景记住偏好**
  - [x] 场景保存/恢复位置、大小、锁定、吸附、安静和频率；可“保存当前偏好”。
  - [x] 仅壁纸不改桌宠偏好；演示意图全局保留；导入导出与旧场景回归覆盖。
  - [ ] 原生场景切换联调。

## 进度与证据

- 基线：已有独立动作片段、透明命中测试、不抢焦点、75%–150%新刻度和无悬浮按钮。
- 开始时共享工作区有其他改动：Provider/Home/Onboarding；本任务不修改或提交这些无关文件。
- 尚未声称五项完成。
- 第1项首次尝试：CUA返回Mac锁屏，未执行UI操作。保留待验收；先推进第2项的实现和可重复几何测试，最后集中回归。
- 后续独立原生测试：设置和桌宠页能单击进入，锁定、安静、演示开关成功设置为开启；重启后的完整复查再次被Mac锁屏阻挡。
- 早期 system_profiler 报告1台显示器；多屏/断开逻辑通过模拟几何测试，真实插拔未测。2026-09-12 已复核为2台，见后续记录。
- 自动回归已覆盖场景快照和壁纸生成并发：只改行为偏好不误判为壁纸绑定变化，真正的视觉绑定变化仍拒绝覆盖。
- 全量前端测试有两项侧栏源码断言失败（非本次修改）；桌宠定向测试单独运行。
- 独立原生测试数据复核：enabled仍为true；位置已保存；positionLocked、quietMode、presentationMode均为true，动作间隔45秒；显示意图未被演示隐藏改写。
- 本轮桌宠定向前端测试、Rust场景/位置/内置升级测试、Skill种子测试、类型检查和正式应用打包通过。并行眨眼改动未纳入本轮提交，分离后的前端暂存版本也通过类型检查。
- 实现完成不等于五项整体完成：剩余未勾选项需要原生现场验收，不能用几何测试代替真实多屏操作。

## 续接验收（2026-09-12）

- 正式宠物库已有奶糖、布丁及6个场景，不重复生成或安装；本轮未改正式宠物、场景或凭证。
- 使用独立 `Astro Pet Motion QA.app` 与 marker-guarded 测试 home。原生主窗单击进入设置、桌宠、通用设置和返回宠物库均成功；这不等于桌宠在其他应用上方的焦点/透明穿透已验收。
- 演示恢复：点击“恢复显示”后从“暂时隐藏”变为“显示中”，测试状态 `presentationMode=false`、`enabled=true`；显示意图未被改写。
- 位置重置：点击“回到屏幕内”后测试状态 `position=null`；没有取得浮动宠物的实际屏幕位置证据，不把状态清除当作物理位置通过。
- 当前硬件为内置 Liquid Retina XDR 3024×1964 与外接5120×2880两屏。跨屏拖动、DPI切换及物理断开仍未测试。
- 场景偏好往返测试开始前再次锁屏，未继续操作；大小75%/100%/150%、原生拖动、右键、全屏/托盘、重启恢复与场景联调保留待验收。
- 动作审查发现踩奶和舔爪的脚底基准变化均为0px、首尾中立帧逐字节相同，但中立帧到第1动作帧仍有头部/眼睑跳变。见 [动作审查报告](desktop-pet-motion-review-20260912.md)。生成慢放文件不等于已经完成慢放播放验收。
- 已授权的 Azure 图片补帧请求在返回响应前断开，未收到新素材；停止后续请求，现有动作未替换。待服务恢复且两条入口一起验收通过后再应用。
- 已结束本轮 Motion QA 进程，测试数据和审查产物保留；未结束另一任务的 QA，也未重启正式应用。
- 新增只读动作审查工具的5项测试通过；重新生成联系表/慢放/测量成功。清除 QA 环境绑定后，普通应用的 TypeScript、Vite、CSS layer检查与 Tauri debug app bundle 构建通过。构建使用当前共享工作区，不代表其他任务的未提交修改已由本轮完整验收；未将这些修改纳入本轮提交。
