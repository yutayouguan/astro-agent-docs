# 布丁：奶油色垂耳小狗

源素材与 QA：`output/imagegen/pudding-dog-20260910/`。不自动替换当前桌宠，不生成额外壁纸。

- [x] 定稿：奶油色金毛幼犬、焦糖垂耳、圆脸短腿，无饰品。
- [x] 定位生成链路：认证正常；长请求遇到休眠/截断；此 Azure 部署仅接受 PNG/JPEG，不接受 WebP。使用临时 caffeinate 保活与 JPEG100 传输后成功，本地输出透明 WebP。
- [x] 动作目录：设置/右键/自动播放统一读实际动作，保留猫咪原动作；不让小狗继承踩奶。
- [x] 内置注册改为逐 ID 检查，加入完整素材前不发布布丁条目。
- [x] 标准 9 行生成、左行逐帧镜像、修正无道具专注行与全坐姿失落行。
- [x] 独立动作源：tail-wag 24 姿态、head-tilt 20 姿态、stretch 24 姿态、nap 24 姿态。
- [x] 温和的 16 个注视方向、最终图集清理及独立视觉 QA（保留轻微观感备注）。
- [x] 四动作最终 bookends/时序与白黑棋盘背景 QA。
- [x] 真实素材内置 `builtin-pudding`、导入导出/场景测试。
- [x] QA 应用打包、前端交互验收。
- [ ] 解锁后完成原生单击、拖动、透明穿透、焦点与实时观感验收。

## 已确认的素材处理决定

- 24 帧排版参考会诱发模型复制网格和编号，改为仅附身份/neutral参考后重生；拒绝版本保留但不装包。
- `running-left` 直接逐帧镜像已抽出的 clean right 帧，不能对原生成横条硬切后镜像，后者会夹带相邻身体碎片。
- 动作使用统一比例而非每帧独立缩放；首尾复用主图集的真实 neutral，元数据标记 `neutralBookends`。
- tail-wag 丢弃缩小的源20–22，使用真实姿态往返退出：39播放位置，19个生成姿态＋neutral。
- head-tilt 重排为单次左右往返：24播放位置，12个生成姿态＋neutral。不是24个新生成帧。
- nap 用身体中心对齐躺姿，剔除过宽的源13/15；22播放位置，20个生成姿态＋neutral，循环时段独立定义。
- 配方在 `motion-recipes.json`，最终 `.qa.json` 记录源索引、真实使用数量、比例、书挡替换与对齐方式。

未使用模型降级、密钥轮换、永久电源配置修改或占位宠物。所有源图与最终素材的身份、透明边缘和连贯性验收通过后才注册到真实库。

## 最终验证（2026-09-11）

- 主图集 `1536×2288 / RGBA / v2` 校验通过，透明 RGB 残留为0；最终边缘处理保持 alpha 不变。
- 独立视觉复核：16方向均通过或仅有minor warning，无反象限、身份更换、裁切或明显透明孔洞。180/315附近有轻微姿态差，部分轮廓在深色底可能有细暖亮边，不宣称任何底色下绝对无边缘感。
- 动作正常/3倍慢速、白/黑/棋盘底检查通过；伸懒腰与睡醒的部分动作节奏保留真人观感复核项。
- types 全部90项、native desktop_pet15项、builtin_pet9项、Pudding专项2项、装配脚本2项测试通过。
- 前端910项：908通过；既有 sidebarInformationArchitecture/windowChromeSafeArea 两项失败保持不动。
- Playwright5项通过，包括两只宠物同时出现、狗动作无猫动作、浏览不替换奶糖、新增狗场景与显式应用。额外像素断言验证打盹确实进入趴姿，不只检查按钮状态。
- 修复继承默认偏好的导出：包内保存实际生效的配置，原场景仍保持继承，不修改当前桌面。
- 原生QA包构建成功；CUA明确报告Mac锁屏，未绕过，未改变正式桌宠选择。
- 正式 Astro Agent.app 已完成最终重建（不绑定 QA 数据根）；暂存区独立 TypeScript 检查通过。
- 宠物库缩略图只解码主图集，进入动作预览才加载动作片段；切换宠物时重置旧动作与右键菜单。
- 完整可导入包：`output/imagegen/pudding-dog-20260910/package/pet.json`。

提交只包含布丁相关改动；已有连续眨眼、侧栏、引导页、Provider和home改动保留在工作区。

## 摇尾巴主体漂移修复（2026-09-11）

用户反馈整只狗左右漂移后复核：旧素材脚掌区域中心跨度约13px；AI整身帧有身体变化，底部轮廓锚点也受尾巴摆动影响。

已改为固定 neutral 身体＋独立尾巴纹理的图层烘焙：49帧，40ms主体采样，尾根固定，平滑进入/循环/退出。不是新增49张AI图；本轮模型请求为0。头像、身体、脚掌与抗锯齿边缘在每帧保持同一像素值。

- 原生Rust资产像素检查、Python逐帧检查、浏览器真实Canvas连续采样均验证身体变化为0，尾巴确实变化且不脱离身体。
- 资源URL改为 `tail-wag-v2.webp`，避免旧图像解码缓存。
- 只升级旧SHA256、旧时序及主图均匹配的内置布丁；旧文件保留，自定义素材/时序及目标路径冲突不覆盖。名称、大小、位置、暂停、壁纸联动和场景保持不变。
- 其余主图集、歪头、伸懒腰、打盹资源未改动。
- 回退与验证产物：`output/imagegen/pudding-tail-fixed-20260911/`。Mac锁屏，原生重启验收仍待解锁。
- 修复验证：Python素材/装配6项、Rust尾巴更新4项、内置宠物模块13项通过；浏览器连续采样身体像素变化0且尾巴变化。正式应用包已重建。
